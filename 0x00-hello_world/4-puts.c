#include <stdio.h>
/**
 * main - entry point
 * receives var of type char and prints that out to std out with puts
 * return - always 0
 */
int main(void)
{
	char firstc[] = "Programming is like building a multilingual puzzle";

	puts(firstc);

	return (0);

}
