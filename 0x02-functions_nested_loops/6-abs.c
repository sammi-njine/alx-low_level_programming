#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 *
 * @n: integer value being checked
 *
 * Return: n which is the absolute value
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
